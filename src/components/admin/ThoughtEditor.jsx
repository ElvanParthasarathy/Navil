import React from 'react';
import { StandardListEditor } from './StandardListEditor';

export const ThoughtEditor = (props) => {
    return <StandardListEditor {...props} collection="thoughts" />;
};
